apiVersion: batch/v1
kind: Job
metadata:
  name: sample-job
  namespace: jobs
spec:
  template:
    spec:
      containers:
        - name: hello
          image: busybox
          command: ["echo", "Running on Fargate!"]
      restartPolicy: Never